{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "alembic upgrade head && python -c 'from app.main import app; print(\"App imported successfully\")' && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level debug",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}